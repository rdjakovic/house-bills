<UserControl x:Class="HouseBills.Wpf.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:HouseBills.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ReportsViewModel}"
             d:DesignWidth="1000"
             d:DesignHeight="700">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Title}" Style="{StaticResource PageTitleStyle}" />

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Year" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" MinWidth="100" Margin="0,0,8,0" />
            <Button Content="Show" Command="{Binding RefreshCommand}" Style="{StaticResource ToolbarButtonStyle}" />
        </StackPanel>

        <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <StackPanel Margin="0,0,32,0">
                <TextBlock Text="Total due" Opacity="0.8" />
                <TextBlock Text="{Binding YearTotal, StringFormat=C}" FontSize="20" FontWeight="SemiBold" />
            </StackPanel>
            <StackPanel Margin="0,0,32,0">
                <TextBlock Text="Paid" Opacity="0.8" />
                <TextBlock Text="{Binding YearPaid, StringFormat=C}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource StatusPaidBrush}" />
            </StackPanel>
            <StackPanel Margin="0,0,32,0">
                <TextBlock Text="Outstanding" Opacity="0.8" />
                <TextBlock Text="{Binding YearOutstanding, StringFormat=C}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource StatusOverdueBrush}" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Previous year" Opacity="0.8" />
                <TextBlock Text="{Binding PreviousYearTotal, StringFormat=C}" FontSize="20" FontWeight="SemiBold" />
            </StackPanel>
        </WrapPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" Margin="0,0,16,0">
                <TextBlock DockPanel.Dock="Top" Text="By month" Style="{StaticResource SectionTitleStyle}" />
                <DataGrid ItemsSource="{Binding Months}" Style="{StaticResource ListGridStyle}" CanUserSortColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Month" Binding="{Binding MonthName}" />
                        <DataGridTemplateColumn Header="" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ProgressBar Maximum="1" Value="{Binding BarFraction, Mode=OneWay}" Height="8" VerticalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Bills" Binding="{Binding Row.BillCount}" />
                        <DataGridTextColumn Header="Total" Binding="{Binding Row.TotalAmount, StringFormat=C}" />
                        <DataGridTextColumn Header="Paid" Binding="{Binding Row.PaidAmount, StringFormat=C}" />
                        <DataGridTextColumn Header="Outstanding" Binding="{Binding Row.OutstandingAmount, StringFormat=C}" />
                        <DataGridTextColumn Header="Previous year" Binding="{Binding Row.PreviousYearTotalAmount, StringFormat=C}" />
                        <DataGridTextColumn Header="Change" Binding="{Binding ChangeFromPreviousYear, StringFormat=C}" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <DockPanel Grid.Column="1">
                <TextBlock DockPanel.Dock="Top" Text="By category" Style="{StaticResource SectionTitleStyle}" />
                <DataGrid ItemsSource="{Binding CategoryTotals}" Style="{StaticResource ListGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="*" />
                        <DataGridTextColumn Header="Bills" Binding="{Binding BillCount}" />
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=C}" SortMemberPath="TotalAmount" />
                        <DataGridTextColumn Header="Paid" Binding="{Binding PaidAmount, StringFormat=C}" SortMemberPath="PaidAmount" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
